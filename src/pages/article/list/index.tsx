import React from 'react';
import { PageContainer } from '@ant-design/pro-layout';
import { Card } from 'antd';

const ArticleListComponent = (): React.ReactNode => {
  return (
    <PageContainer>
      <Card>ArticleListComponent</Card>
    </PageContainer>
  );
};

export default ArticleListComponent;
